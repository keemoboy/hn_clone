<p>
  <b>Description:</b>
  <%= @link.description %>
</p>

<p>
  <b>Url:</b>
  <%= @link.url %>
</p>

<p>
  <b>Site:</b>
  <%= @link.site %>
</p>
<p>
  <b>Comment:</b>
  <%= @head_comment.body %>
</p>





<%= form_for([@subject,@comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <% f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit%>
  </div>
<% end %>

<br />
Comments:
<% @comments.each do |comment| %>
<br /><%= comment.body %><%= button_to "reply", new_comment_comment_path(@subject), :method => :get %>
<% end %>


<%= link_to 'Back', links_path %>
